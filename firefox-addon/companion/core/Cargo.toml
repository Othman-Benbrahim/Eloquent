[package]
name = "eloquent-companion-core"
version = "0.2.0"
edition = "2021"
rust-version = "1.77.2"
license = "GPL-3.0-only"
description = "Cross-platform lifecycle manager for the local Eloquent LanguageTool server"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

